[package]
name = "omnichain-zk"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
omnichain-core = { path = "../omnichain-core" }
omnichain-crypto = { path = "../omnichain-crypto" }
serde = { workspace = true }
rand = { workspace = true }
sha3 = { workspace = true }

# Note: Halo2 dependencies removed temporarily due to git revision issues
# Will be re-added with correct versions in production

[dev-dependencies]
tokio-test = { workspace = true }
